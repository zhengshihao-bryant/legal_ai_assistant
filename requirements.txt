# -*- coding: utf-8 -*-
"""V1 Knowledge RAG Core 依赖清单（Python 3.10+）。

安装：pip install -r requirements.txt
"""
# --- 核心（必须）---
numpy>=1.26
pymupdf>=1.24          # PDF 解析
python-docx>=1.1       # DOCX 解析
jieba>=0.42            # 中文分词（BM25 / 评估）
rank-bm25>=0.2.4       # BM25 Sparse Retrieval
scikit-learn>=1.4      # 评估指标（余弦/聚类等）

# --- 向量化 / Rerank（bge 系列，CPU 即可）---
sentence-transformers>=3.0

# --- 向量数据库（可选：本地回退基于 numpy，无需安装）---
# pymilvus>=2.4        # 使用 Milvus 时安装

# --- LLM API（可选：无 Key 时走本地抽取式生成兜底）---
# openai>=1.30

# --- OCR 兜底（可选：扫描件 PDF 需要；RapidOCR 纯 pip 安装，无需系统程序）---
rapidocr-onnxruntime>=1.3.0
# pytesseract>=0.3.10  # 备选：需额外安装 Tesseract OCR 系统程序
